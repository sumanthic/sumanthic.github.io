# Phase 5 — Navigation Header & Footer: Validation

Validation not required for this phase — implementation is trusted on merge.

## Merge criteria

- `layouts/partials/header.html` and `layouts/partials/footer.html` exist
- Both are wired into `baseof.html`
- CSS for header and footer is present in `main.css`
